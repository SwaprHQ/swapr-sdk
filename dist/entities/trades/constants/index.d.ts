import { ChainId } from '../../../constants';
import { Percent } from '../../../entities/fractions';
/**
 * Default maximum slippage tolerance at 3%
 */
export declare const maximumSlippage: Percent;
export declare const REFFERER_ADDRESS_CHAIN_MAPPING: Partial<Record<ChainId, string>>;
